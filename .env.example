# MongoDB Database Connection
MONGO_STRING=mongodb+srv://username:password@cluster.mongodb.net/database-name?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random
ACCESS_TOKEN_EXP=2h

# Optional: BCrypt Salt Rounds (default: 10)
# BCRYPT_SALT_ROUNDS=12

# Instructions:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual credentials
# 3. For MONGO_STRING: Get this from MongoDB Atlas or your MongoDB provider
# 4. For JWT_SECRET: Use a long, random string (at least 32 characters)
# 5. ACCESS_TOKEN_EXP: Token expiration time (1h, 2h, 8h, etc.)
